[metadata]
name = model-service
version = 0.3.1
description = Service for interacting with the model

[options]
package_dir =
    = src
# Automatically find all packages
packages = find:
# Include non-code files (like VERSION)
include_package_data = True

[options.packages.find]
where = src
